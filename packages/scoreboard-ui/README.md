# Scoreboard UI

Accessible presentation of score, inning half, outs, review/delay overrides, doubleheader identity and quiet state. It renders authoritative snapshots and performs no local score counting.
